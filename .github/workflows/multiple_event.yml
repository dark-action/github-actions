name: multiple-event-workflow
on:
  push:
  issues:
    types: [opened]
  workflow_dispatch:

jobs:
  multiple-event-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "This is step 1 of the multiple event workflow"
      - name: step2
        run: |
          echo hello world
          echo github action
      - name: step3
        run: |
          echo "This workflow can be triggered by push, issue opened, or workflow dispatch events."