name: branch-filter
on:
  push:
    branches: ["dev"]

jobs:
  branch-filter-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "This is step 1 of the branch filter workflow"

