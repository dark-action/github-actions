name: push-workflow-test
on:
  pull_request:   # ← 여기서 하이픈(-)이 아니라 언더바(_)
    types: [opened]

jobs:
  pull-request-job:
    runs-on: ubuntu-latest
    steps:
       - name: step1
         run: echo "This is step 1 of the push workflow"
       - name: step2
         run: |
          echo hello world
          echo github action
