name: push-workflow111
on: pull-request

jobs:
  pull-request-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "This is step 1 of the push workflow"
      - name: step2
        run: |
          echo hello world
          echo github action
          
          pull_request
