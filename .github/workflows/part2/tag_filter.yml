name: tag-filter
on:
  push:
    tags:
      - 'v*'  # Only run on tags that start with 'v'

jobs:
  tag-filter:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: echo "This is step 1 of the tag filter workflow"
    - name: step2
      run: echo "This is step 2 of the tag filter workflow"
    - name: step3
      run: echo "This is step 3 of the tag filter workflow"
    - name: step4
      run: echo "This is step 4 of the tag filter workflow"