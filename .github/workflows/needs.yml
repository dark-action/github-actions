name: needs
on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo " This is step 1 in job1 of the needs workflow"

  job2:
    runs-on: ubuntu-latest
    needs: [job1]
    steps:
      - name: step1
        run: echo " This is job2 of the needs workflow"

  job3:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: |
          echo " This is job3 of the needs workflow fail"
          exit 1

  job4:
    runs-on: ubuntu-latest
    needs: [job3]
    steps:
      - name: step1
        run: |
          echo " This is job2 of the needs workflow"
          exit 1
