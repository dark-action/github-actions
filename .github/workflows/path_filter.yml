name: path-filter
on:
  push:
    paths:
      - '.github/workflows/part1/**'

jobs:
  path-filter:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: echo "This is step 1 of the path filter workflow"
